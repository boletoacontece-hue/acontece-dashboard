# ===== NÃO publicar dados sensíveis no repositório público =====
# backups e exports de dados
seed/
acontece_backup_*.json
*.backup.json

# planilhas (dados reais de imóveis/clientes)
*.xlsx
*.xls
*.csv

# segredos / ambiente
.env
*.secret

# lixo de sistema
.DS_Store
Thumbs.db

# manifest do PWA precisa ir pro site (exceção explícita, por garantia)
!manifest.json