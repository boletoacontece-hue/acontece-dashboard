# ===== NÃO publicar dados sensíveis no repositório público =====
# backups e exports de dados (mantém o intuito original, sem bloquear o manifest)
seed/
acontece_backup_*.json
*.backup.json

# planilhas (dados reais de imóveis/clientes)
*.xlsx
*.xls
*.csv

# segredos / ambiente
.env
*.secret

# lixo de sistema
.DS_Store
Thumbs.db

# Garantir que o manifest do PWA SEMPRE seja publicado
!manifest.json
